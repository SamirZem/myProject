{
  "name": "clash-analyzer-backend",
  "version": "1.0.0",
  "description": "Thin proxy in front of the official Clash Royale API, so the API key (IP-locked) never has to live on the Android device.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "express": "^4.21.1"
  },
  "engines": {
    "node": ">=18"
  }
}
