<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Screen capture for the live match analysis. MediaProjection itself needs no
         "dangerous" permission grant beyond the system capture-consent dialog shown
         each time capture starts; these two declare and authorize the foreground
         service that hosts it, per Android 14+ requirements. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Cleartext (plain HTTP) traffic is allowed because the app only ever talks to the
         user's own self-hosted backend relay (see backend/README.md) — often a plain VPS
         IP with no TLS certificate for a personal-use tool like this. If you put the relay
         behind HTTPS (recommended for anything beyond personal use), this can be removed. -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".ClashAnalyzerApp"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClashAnalyzer"
        android:usesCleartextTraffic="true"

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ClashAnalyzer"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".capture.CaptureForegroundService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

    </application>

</manifest>
