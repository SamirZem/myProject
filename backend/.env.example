# Get this from https://developer.clashroyale.com after registering the *public IP address*
# of wherever this server runs (a VPS, Render/Fly/Railway instance, etc.) — the key is rejected
# from any other IP.
CLASH_ROYALE_API_KEY=

# Optional, defaults to 3000.
PORT=3000
